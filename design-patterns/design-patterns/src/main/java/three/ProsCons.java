package three;

public class ProsCons {

    /**
     *
     * PROS:
     *   * The code is more maintainable if we have more than 4 fields
     *   * Object creation is less error-prone as we use explicit method calls.
     *   *
     *
     * CONS:
     *   * It brings code duplication as we need to implement all fields in builder class.
     *
     *
     */
}
