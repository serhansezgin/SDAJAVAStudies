package homework;

public class Homework {

    /**
     *
     * Research how fetch types and cascade types work ...
     *        (fetch = FetchType.LAZY)
     *        (cascade = CascadeType.ALL)
     *
     */
}
